# /etc/nginx/sites-available/commandcenter.theconradteam.com
# Conrad Command Center + Brain (uvicorn :8000). TLS via certbot on Ubuntu.

server {
    listen 80;
    server_name commandcenter.theconradteam.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name commandcenter.theconradteam.com;

    # After: sudo certbot --nginx -d commandcenter.theconradteam.com
    ssl_certificate     /etc/letsencrypt/live/commandcenter.theconradteam.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/commandcenter.theconradteam.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
    }
}
